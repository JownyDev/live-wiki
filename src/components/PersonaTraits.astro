---
import PersonaTagList from './PersonaTagList.astro';

type Props = {
  items: string[];
};

const { items } = Astro.props as Props;
---

<PersonaTagList label="Rasgos" items={items} icon="psychology" />
