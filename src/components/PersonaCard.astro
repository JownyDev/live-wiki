---
type PersonaCardSpan = 'wide' | 'full';

type Props = {
  title: string;
  span?: PersonaCardSpan;
};

const { title, span } = Astro.props as Props;
const className = span ? `persona-card persona-card--${span}` : 'persona-card';
---

<div class={className}>
  <h3 class="persona-card__label">{title}</h3>
  <div class="persona-card__content">
    <slot />
  </div>
</div>

<style>
  .persona-card {
    padding: var(--space-4);
    border-radius: var(--radius-md);
    border: 1px solid var(--color-border);
    background: var(--color-surface);
    display: grid;
    gap: var(--space-2);
  }

  .persona-card__label {
    margin: 0;
    font-size: 0.7rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    color: var(--color-accent);
  }

  .persona-card__content {
    display: grid;
    gap: var(--space-2);
  }

  @media (min-width: 720px) {
    .persona-card--wide {
      grid-column: span 2 / span 2;
    }
  }

  .persona-card--full {
    grid-column: 1 / -1;
  }
</style>
