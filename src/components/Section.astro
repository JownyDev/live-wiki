---
type Props = {
  title?: string;
};

const { title } = Astro.props;
---

<section class="pb-5 ">
  {title ? <h2 class="text-[1.5rem] mb-space-3 font-serif leading-tight tracking-tight">{title}</h2> : null}
  <slot />
</section>